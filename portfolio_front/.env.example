# Copy this to .env and fill in your values

# Laravel API base URL (leave empty to use static data only)
VITE_API_URL=http://localhost:8000

# Admin panel password (for standalone use without Laravel auth)
# When using Laravel, auth is handled by the API
VITE_ADMIN_PASSWORD=

# App environment
VITE_APP_ENV=development
